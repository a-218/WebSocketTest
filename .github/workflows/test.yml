on:
    push:
    workflow_dispatch:
    

jobs: 
    tests:
        runs-on: ubuntu-22.04
        steps:
           - uses: actions/checkout@v3
           - name: Setup dotnet
             uses: actions/setup-dotnet@7
             with:
                dotnet-version : 8.x
           - run: cd Test && dotnet test
           